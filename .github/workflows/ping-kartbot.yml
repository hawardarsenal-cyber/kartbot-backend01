name: Keep kartbot-backend01 hot

on:
  schedule:
    - cron: "*/5 8-23 * * *"   # Every 5 mins, 08:00–23:55 UK
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render service
        run: |
          curl -fsS https://kartbot-backend01.onrender.com/healthz || true
